import * as React from "react";
import { PushMenu } from "src/components/Layouts";

const Page: React.SFC<{}> = props => {
  return (
    <div>
      <PushMenu />
    </div>
  );
};

export default Page;
